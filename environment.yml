name: fair_intro_env
channels:
  - defaults
dependencies:
  - python=3.7
  - matplotlib
  - numpy
  - pandas
  - scipy
  - pip:
    -tqdm
  #   - fair==1.6.1
prefix: /opt/anaconda3/envs/fair_intro_env
